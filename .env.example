# ESPHome Device Builder dashboard URL (REST + WebSocket are derived from this).
# Use a stable hostname so the container survives moving between hosts.
ESPHOME_DASHBOARD_URL=https://esphome.strant.casa

# Direct-to-dashboard alternative (bypasses the reverse proxy):
# ESPHOME_DASHBOARD_URL=http://soundwave.strant.casa:6052

# Optional Basic Auth — only needed if the dashboard reports requires_auth=true.
# ESPHOME_DASHBOARD_USERNAME=
# ESPHOME_DASHBOARD_PASSWORD=

# Optional log level (DEBUG, INFO, WARNING, ERROR).
# LOG_LEVEL=INFO
